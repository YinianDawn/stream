// 文件命名风格
// 偏向于定义函数的文件 采用 小写和下划线命名
// 偏向于定义类的文件   采用 驼峰命名法

// 本文件为内置函数 没有包名 表是默认引入
// 所有没有包名的文件都是默认引入的

import canoe.lang.String    // 这里导入的是 canoe.lang 包下的String类

// 该函数名为 print
// 接受参数为 x String类型
print(x :String) {
    // TODO 这里实现输出内容, 也许是调用个本地方法
    ...
} :public
